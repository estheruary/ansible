---

- hosts: localhost
  connection: local
  tasks:
    - debug: msg="Starting the do thing"

    - name: Test my do block.
      do:
        - debug: var=item

        - debug: msg=two
      loop: [hello, world]


    - debug: msg="Finished do block"
